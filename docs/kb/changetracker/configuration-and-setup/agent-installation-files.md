---
description: >-
  Describes the files stored in the Netwrix Change Tracker Gen 7 NetCore agent
  installation path, including Events.db, Config.xml, Rolling-log.txt, and
  HubDetails.xml.
keywords:
  - agent files
  - Netwrix Change Tracker
  - agent installation
  - Events.db
  - Config.xml
  - Rolling-log.txt
  - HubDetails.xml
  - ProxiedDevices.json
  - Store.db
  - gen7agent.app.netcore
products:
  - changetracker
knowledge_article_id: kA04u0000000JbnCAE
sidebar_label: Understanding the Agent Installation Files
tags: [kb, configuration-and-setup]
title: Understanding the Agent Installation Files
---

# Understanding the Agent Installation Files

## Overview

This article describes the files found in the Gen 7 NetCore agent's installation path and what each one is used for.

The current Gen 7 NetCore agent stores its working data in the following locations:

- **Windows:** `C:\ProgramData\NNT\gen7agent.app.netcore\`
- **Linux:** `/var/nnt/gen7agent.app.netcore/`

## Instructions

### Agent Files

- **Agent.id** — Contains the unique ID the agent receives during installation. If two devices register with the same name but different IDs in this file, the hub returns an error — see [Error: Hub Connection Failed (500 Server Message)](../troubleshooting-and-errors/error-hub-connection-failed-500-server-message.md) to resolve it.
- **MasterDevice.json** — The details of the master proxy device, which is always the local agent, including the parameters the agent uses to interact with any remote systems or databases it monitors.
- **ProxiedDevices.json** — An array of the devices currently proxied by this agent. Devices that cannot run the agent directly are proxied instead, and the agent records their names here.
- **Events.db** — The events data captured by the agent.
- **Config.xml** — The device's configuration data, including which directories, registry items, and security policies the agent tracks.
- **Store.db** — The agent's baseline information.
- **Rolling-log.txt** — Log data generated by the agent. Useful for troubleshooting, since it records errors, compliance report runs and any errors in them, and when the agent goes online or offline. For more detail on this file, refer to [Rolling Log File](pathname:///docs/changetracker/8_2/install/agent/rollinglogfile).
- **HubDetails.xml** — The connection details the agent uses to authenticate to the Netwrix Change Tracker hub, including the username, password, hub URL, and proxy information.
