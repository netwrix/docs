---
title: "Mixed Vale and Dale Test"
description: "Test page with both Vale and Dale violations"
sidebar_position: 989
---

# Mixed Vale and Dale Test

This file has both Vale violations (Phase 1 + Phase 2) and Dale violations (Phase 3). The summary should combine all phases.

## Vale Phase 1 Violations

ensure you select the correct checkbox in the dropdown menu before proceeding.

You don't need to click the button. The system doesn't require authorization.

to use the feature, you will need to set up the configuration.

configure the connection string before continuing.

## Dale Violations

The monitoring agent was installed on the server by the IT team after the configuration was reviewed.

Using the admin console, the security policies are applied automatically across the network.

You can easily deploy the agent to hundreds of endpoints in just a few minutes with minimal effort.

Without Access Analyzer, you have no way to audit file permissions across your organization.

In the event that you need to modify the configuration, you should navigate to the administration page to make the necessary changes to the system.

## Mixed on Same Lines

ensure that the configuration file was updated by the administrator before deployment.

:::note
The monitoring plan was configured by the operations team.
:::

to use the export tool, the reports are generated by the system automatically.
