---
title: "Mixed Vale and Dale Test"
description: "Test page with both Vale and Dale violations"
sidebar_position: 989
---

# Mixed Vale and Dale Test

This file has both Vale violations (Phase 1 + Phase 2) and Dale violations (Phase 3). The summary should combine all phases.

## Vale Phase 1 Violations

ensure you select the correct checkbox in the dropdown menu before proceeding.

You don't need to click the button. The system doesn't require authorization.

to use the feature, you will need to set up the configuration.

configure the connection string before continuing.

## Dale Violations

The IT team installed the monitoring agent on the server after reviewing the configuration.

The admin console applies the security policies automatically across the network.

You can deploy the agent to hundreds of endpoints in a few minutes.

Use Access Analyzer to audit file permissions across your organization.

If you need to modify the configuration, navigate to the administration page.

## Mixed on Same Lines

ensure that the administrator updated the configuration file before deployment.

:::note
The operations team configured the monitoring plan.
:::

to use the export tool, the system automatically generates the reports.
