:::warning Ignore Historization
When `IgnoreHistorization` is set to `true`, the task will not create historical records of changes. This can improve performance but reduces audit trail capabilities.
:::
